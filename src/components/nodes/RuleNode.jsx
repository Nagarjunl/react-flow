import React from 'react';

// Placeholder component - not used in current implementation
// Rule functionality is handled by resizableGroup + ruleName combination
const RuleNode = () => {
    return (
        <div style={{
            padding: '10px',
            background: '#f0f0f0',
            border: '1px dashed #ccc',
            borderRadius: '4px',
            textAlign: 'center',
            color: '#666'
        }}>
            Rule Node (Deprecated)
        </div>
    );
};

export default RuleNode;
