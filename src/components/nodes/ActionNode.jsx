import React from 'react';

// Placeholder component - not used in current implementation
// Action functionality is handled by resizableGroup + actionName combination
const ActionNode = () => {
    return (
        <div style={{
            padding: '10px',
            background: '#f0f0f0',
            border: '1px dashed #ccc',
            borderRadius: '4px',
            textAlign: 'center',
            color: '#666'
        }}>
            Action Node (Deprecated)
        </div>
    );
};

export default ActionNode;
